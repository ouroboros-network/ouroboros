[package]
name = "simple_dex"
version = "0.1.0"
edition = "2021"
description = "Simple DEX (AMM) contract for OVM - supports 18-decimal tokens"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
